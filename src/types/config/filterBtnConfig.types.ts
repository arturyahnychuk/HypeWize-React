export interface filterBtnConfigTypes {
  text: string;
  icon?: boolean; // if true button has icon
  name?: string; // if icon is true than we have to add name to icon
  width?: number;// for icons if button has icon
  height?: number;// for icons if button has icon
  value: string;
  active?: boolean;
}
